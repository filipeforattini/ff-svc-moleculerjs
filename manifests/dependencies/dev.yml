#@data/values
---
mysql:
  enabled: true
  values:
    primary:
      persistence:
        enabled: false

postgres:
  enabled: true
  values:
    primary:
      persistence:
        enabled: false

redis:
  enabled: true

nats:
  enabled: true

etcd:
  enabled: true
