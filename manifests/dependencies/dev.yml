#@data/values
---
mysql:
  enabled: true
  values:
    primary:
      persistence:
        size: 1Gi

postgres:
  enabled: true
  values:
    primary:
      persistence:
        size: 1Gi

redis:
  enabled: true

nats:
  enabled: true

etcd:
  enabled: true
